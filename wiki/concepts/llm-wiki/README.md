# LLM-Wiki concepts

Durable concepts about how LLM-Wiki itself works.

## canonical concepts

- [[4-stage-lifecycle.md]] — concept → pre-repo → real repo → file-back loop. The
  foundational lifecycle model for any project that flows through the vault.

## active drafts

- Retired Hermes concepts were archived under
  `archive/retired-hermes/2026-06-30/`. Active LLM-Wiki coordination now uses
  `TICK.md`, task packets, session packs, and Markdown evidence.

## visual model

Refreshed visual source created for `LLMWIKI-020`:

- `operating-model.mmd` / `operating-model.svg`
- `concept-flow.mmd` / `concept-flow.svg`

Use the Mermaid files as editable source. The SVG files are static exports for
quick reading.
